#include "PixelDisplayer.h"

void PixelDisplayer::displayPixels(const std::vector<std::vector<Color>>& pixels)
{
	for (size_t i = 0; i < pixels.size(); ++i)
	{
		for (size_t j = 0; j < pixels[0].size(); ++j)
		{
			// displayPixel(pixels[i][j]);
		}
	}
}
